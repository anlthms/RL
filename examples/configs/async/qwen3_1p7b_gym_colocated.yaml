# Qwen3-1.7B dense, NeMo-Gym env, colocated async GRPO (Megatron inference).
# Model, sequence length (16384) and reasoning/tool parsers come from
# qwen3_1p7b_base.yaml.
# Launch: MODEL=qwen3_1p7b ENV=gym bash launch_experiment.sh colocated
defaults:
  - qwen3_1p7b_colocated.yaml
  - env_gym.yaml
