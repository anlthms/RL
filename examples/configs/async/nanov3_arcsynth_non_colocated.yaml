# Nano-v3 MoE, synthetic ARC-ladder env, non_colocated async GRPO.
# Launch: MODEL=nanov3 ENV=arcsynth bash launch_experiment.sh non_colocated
defaults:
  - nanov3_non_colocated.yaml
  - env_arc_synth.yaml
