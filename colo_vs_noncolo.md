# Colocated vs Non-colocated RL

## Goal

Determine which mode delivers better training efficiency for async GRPO with Nemo-RL (Megatron inference backend) on a fixed GPU budget.

The metric to watch is **validation/total\_reward/mean** which is a good proxy for the skill of the model in verifiable tasks. Caveat: if the session uses a dynamic curriculum that adjusts the difficulty of the task according to the current reward, this metric wouldn’t be useful for comparison purposes (TODO: check if this is the case).

We launch both runs from the same model checkpoint, use the same data and run for the same duration on the same number of nodes. Higher mean reward indicates more learning per GPU-hour.

## Background

### Sync mode

This implements the classic alternating loop. Each step generates rollouts for one batch with the current weights, computes logprobs, trains, then refits the updated weights into the generation engine before the next step. All training data is on-policy (generated by the weights being updated). The cost is idle hardware: while training runs, the generation side sits idle, and vice versa.

### Async mode

Generation and training run concurrently. A background AsyncTrajectoryCollector produces rollouts continuously, tagging each trajectory group with the weight version that generated it, and deposits them into a ReplayBuffer. The training loop samples groups from the buffer, accepting trajectories up to max\_trajectory\_age\_steps versions old (defaults to 4). Because the data may lag the policy by a few steps, the loss requires importance sampling correction (loss\_fn.use\_importance\_sampling\_correction=true). With in\_flight\_weight\_updates=true, weights are refit to the inference pool after each training step. Generation never stops if the node pools are non-colocated.

### Non-colocated

Training and inference pools are separate. For example, an 8-node allocation may be split:

- 4 nodes x 4 GPUs \= 16 training GPUs  
- 4 nodes x 4 GPUs \= 16 inference GPUs

Training and inference can run concurrently (in async mode). After each training step, updated weights are transferred to the inference nodes. The inference nodes have all of their HBM dedicated to the KV cache and model weights with no competition from optimizer state, gradients etc.

### Colocated

Training and inference share the same pool:

- 8 nodes x 4 GPUs \= 32 shared GPUs (Note that this allows more parallelism)

The number of nodes is effectively double, which means more data can be processed in parallel. While alternating between modes, buffers are offloaded so that there is no contention for memory.

## Hypothesis

We expect the colocated mode to be more efficient. In non-colocated mode, the policy lag is exacerbated because training continues while inference is in progress. This could affect training dynamics adversely.

## What does “Async colocated” mean?

All nodes keep alternating between training and generation when they are colocated. This implies that colocation is arguably synchronous with respect to GPU execution. 

So what do we really mean by “async colocated”?

Answer: it is async in terms of RL dataflow. The async collector/replay buffer can generate trajectories with a certain weight version, store them, and target them for multiple future training steps. A rollout can span several weight versions before being consumed. That is different from “sync colocated”, where each step is more like: generate exactly this step's rollouts, immediately train on them, repeat.

## Method

We use a Nemotron-3 Nano (30B total, 3B active) checkpoint right after the SFT stage. For data, we use the multi-env RL dataset made public by Nemo-RL.

For both experiments, we use a code snapshot from Nemo-RL main (commit hash f2ab62c) pulled on July 6th 2026, with patches applied as described in [this slack thread](https://nvidia.slack.com/archives/C07SMJJDRLN/p1782331212225949). The commit from [PR 2884](https://github.com/NVIDIA-NeMo/RL/pull/2884/changes) is also applied on top of this codebase to make it ready for async colocated mode with Megatron backend.

For both configs, we set the following:

| grpo:   num\_prompts\_per\_step: 16 policy:  train\_global\_batch\_size: 256 env:   nemo\_gym:     skip\_venv\_if\_present: true  |
| :---- |

The global batch size and prompts per step are proportionately scaled down from their base values of 1024 and 64 respectively. This is to make sure that we can run an RL session with 8 nodes in the OCI-HSG cluster (each node has four GB200 GPUs) without running into OOM issues.

### Config for non-colocated

|     colocated:       enabled: false       resources:         gpus\_per\_node: 4         num\_nodes: 4  |
| :---- |

### Config for colocated

|     colocated:       enabled: true       resources:         gpus\_per\_node: 4         num\_nodes: null  |
| :---- |

The commands to launch RL are given below, with differences indicated in bold.

### Command line for non-colocated:

| COMMAND='mkdir \-p /tmp/nemo\_rl\_triton\_cache && TRITON\_CACHE\_DIR=/tmp/nemo\_rl\_triton\_cache NETRC=/home/anthomas/.netrc NRL\_MEGATRON\_CHECKPOINT\_DIR=/lustre/fsw/portfolios/nemotron/users/anthomas/checkpoints\_non\_colocated NEMO\_GYM\_VENV\_DIR=/lustre/fs1/portfolios/nemotron/projects/nemotron\_sw\_pre/users/anthomas/gym\_venvs uv run examples/nemo\_gym/run\_grpo\_nemo\_gym.py \--config examples/nemo\_gym/async\_non\_colocated\_nanov3.yaml policy.generation.backend=megatron logger.wandb.name=async\_non\_colocated' bash submit\_nemorl.sh |
| :---- |

### Command line for colocated:

| COMMAND='mkdir \-p /tmp/nemo\_rl\_triton\_cache && TRITON\_CACHE\_DIR=/tmp/nemo\_rl\_triton\_cache NETRC=/home/anthomas/.netrc NRL\_MEGATRON\_CHECKPOINT\_DIR=/lustre/fsw/portfolios/nemotron/users/anthomas/checkpoints\_colocated NEMO\_GYM\_VENV\_DIR=/lustre/fs1/portfolios/nemotron/projects/nemotron\_sw\_pre/users/anthomas/gym\_venvs uv run examples/nemo\_gym/run\_grpo\_nemo\_gym.py \--config examples/nemo\_gym/async\_colocated\_nanov3.yaml policy.generation.backend=megatron logger.wandb.name=async\_colocated' bash submit\_nemorl.sh |
| :---- |

### Comparison

Use the mean total reward on the validation set as a metric to compare the two runs. As we are comparing efficiency of learning, run both modes for the same duration and then compare them.

## Results

### [Non-colocated (baseline)](https://wandb.ai/adlr/nano-v3-megatron-inference/runs/a28k1rl8):

![][image1]  

### Colocated: see "E4 comparison result" below (continuous scheduler, offline validation)

## Findings (2026-07, colocated single-model campaign)

This section summarizes a follow-up campaign on branch `async_colo1`. Full ledger in
`experiment_log.md`; plans in `colo_single_model_plan.md` and `colo_decode_fast_plan.md`. All jobs
launched via `launch_colo_experiment.sh` / `launch_gentrace_experiment.sh` / `run_offline_validation.sh`
on Slurm account `nemotron_sw_post`; driver logs at `<jobid>-logs/ray-driver.log`.

### 1. Single dual-mode model works (colocated fast decode without a 2nd model)
- Megatron's `inference_optimized` layers are **dual-mode**: fast inference kernels when the engine
  is active, trainable Transformer-Engine fallback otherwise. So ONE model can both train and
  generate — no separate inference model, no weight sync. (Alternative to the two-model
  `colo_decode_fast_plan.md`.)
- Implemented via `policy.megatron_cfg.transformer_impl=inference_optimized` +
  `allow_inference_optimized_training` (relaxes a self-imposed guard in `megatron_policy_worker.py`)
  + registering `InferenceColumnParallelLinear` with megatron-bridge's AutoMapping (needed because the
  training model is now the refit **export source**). Config: `async_colocated_singlemodel_nanov3.yaml`.
- Result (E1b, job 4564982): the single model trains AND generates in the colocated loop; rollout
  decode ~**7685 tok/s/GPU** (parity with non-colo ~7400), vs ~640 for the old reuse-the-TE-model path.

### 2. Colocated VALIDATION decode is ~7x slower than non-colo — root cause found
- At matched batch (~40), colo decode = ~12–13 engine steps/s vs non-colo ~88 (measured with an
  opt-in `NRL_GEN_TRACE` per-step trace patched into the engine from `megatron_worker.py`). It is a
  fixed per-step overhead, so it only bites at LOW batch (validation); high-batch rollouts hide it.
- Ruled out by direct measurement: KV/memory pressure (`paused=0`/`waiting=0` always), concurrency,
  idle time, and graphs-off (both use CUDA graphs).
- Root cause: per-layer CUDA-graph managers are created at BUILD only when `cuda_graph_impl='local'`.
  The colo model is the reused TRAINING model (built `'none'`, graphs toggled at runtime), so its
  graphs are weak; the non-colo DEDICATED inference model is built `'local'`/`'block'`/no-recompute.
- A single-model fix is NOT viable: building the shared model with inference graphs conflicts
  irreconcilably with training (`activation_checkpointing` / full-recompute vs local graphs). A
  dedicated inference model would fix it but reintroduces two-model complexity. **Note:** the standard
  `transition_moe_cudagraphs` fix does not apply here — nano-v3 is Nemotron-H (Mamba hybrid), not
  `MoETransformerLayer`.

### 3. Fair throughput comparison (validation moved OFFLINE) — non-colo ~2x
- To avoid the colo validation penalty contaminating the comparison, validation was removed from the
  training loop (`val_period=0`), checkpoints saved every 5 steps (weights only), and validation run
  **offline** on saved checkpoints. Configs: `noval_{colocated_singlemodel,non_colocated}_nanov3.yaml`.
- Result (4h each): **non-colo 39 optimizer steps** (full duration); **colo 19 steps**, then killed by
  the idle-GPU reaper. Colo replay buffer **STARVES** (`Insufficient valid groups… waiting for
  buffer`): its slower generation can't feed training fast enough → training stalls → GPUs idle. So
  the E2b decode slowness manifests at the training level as buffer starvation. **Non-colocated is the
  more efficient arm here** (~2x optimizer steps).
- **2026-07-12 re-analysis: this conclusion is an artifact of the async scheduler, not of colocated
  capacity.** In job 4736532, training cost only ~15s/step; the rest was buffer waits (68% of
  wall-clock), arriving as one ~31-37 min full-rollout-latency stall every 4-5 steps
  (= the `max_trajectory_age_steps=4` window), with sampled trajectories pinned at the maximum age
  4.0. The collector self-paused once per step after spawning its 4-target window (in-flight cap =
  `num_prompts × max_age` = 64 = window capacity), so during each stall the in-flight pool only
  drained — engine concurrency decayed to a straggler tail, which is the "idle GPUs" the reaper
  killed. At E1b decode parity (7685 tok/s/GPU), colo's 32 GPUs could produce a batch in ~185s of
  saturated generation (~60 steps/4h achievable vs 19 observed). See "Ideal colocated execution
  model" below for the intended workflow.

### 4. Offline validation harness
- Train-batch reward is **curriculum-confounded** (difficulty rises with step), so a fair reward
  comparison requires the fixed val-split. `grpo.validate_only` (new flag) resumes a checkpoint,
  validates via `val_at_start`, and exits before training. `run_offline_validation.sh` submits one
  small (2-node) job per checkpoint. Must run in **colocated** single-pool mode: keeps all GPUs busy
  (the idle-GPU reaper kills the non-colo split) and the non-colo async coordinator hangs without the
  training collector. Slow (~44 min/ckpt) but that is irrelevant offline.
- Reward-vs-step curve (offline val accuracy on the fixed val-split, both arms) is the clean
  efficiency metric; results in `experiment_log.md` (first point: non-colo step_5 accuracy=0.261).

## Ideal colocated execution model (design target, 2026-07-12)

The E3 numbers above measured a particular scheduler, not colocation itself. This section pins down
the execution model that colocated async GRPO *should* implement, so future experiments test the
right thing.

### The two-phase loop

**Generation phase — keep every GPU busy.**
All GPUs generate. The engine is kept saturated: the moment one rollout finishes, another starts.
There is no target-step gating, no lookahead window that exhausts, and no collector self-pause —
the only reason a GPU idles during generation is that the phase is about to end. Completed
per-prompt groups accumulate in the replay buffer.

**Switch to training — triggered from the generation side.**
The phase ends when the buffer holds enough *complete* groups for a training step
(`num_prompts_per_step`). Rollouts still in mid-flight are **paused in place**: the engine suspend
retains every in-flight request and its progress (this part already works — the Megatron dynamic
engine reaches a barrier-synchronized PAUSED state and keeps its requests; see the Phase A note in
`AsyncTrajectoryCollector.prepare_for_refit`). Generation buffers (KV cache etc.) are offloaded so
training has the full HBM.

**Training phase — never waits.**
Training consumes only groups that are already complete. By construction it never blocks on the
buffer: nothing is generating during training, so **any buffer wait in training mode is a scheduler
bug, not a tuning problem** — it would wait forever. If the buffer holds more than one complete
batch, multiple optimizer steps may run back-to-back before switching.

**Switch back to generation.**
Weights are updated (with the single dual-mode model there is no transfer — the trained weights are
the inference weights), the engine resumes, and the paused rollouts continue **from the exact token
where they stopped**, now decoding with the new weights.

### Correctness: rollouts spanning multiple weight versions

A resumed rollout has a prefix sampled under version N and a continuation sampled under N+1 (or
later). This is fine: each token's generation logprob is recorded at sampling time, so the
behavior-policy logprobs are exact per token even for mixed-version sequences, and the token-level
importance-sampling correction (`loss_fn.use_importance_sampling_correction=true`) needs no special
casing. Continuing on the pre-update KV cache is the standard Magistral-style approximation;
AREAL-style cache recompute is available via `recompute_kv_cache_after_weight_updates` if it ever
matters.

### Where the current implementation diverges (all observed in job 4736532)

1. **The switch is driven from the wrong side.** Training polls `ReplayBuffer.sample()` and
   busy-waits until a batch appears; the generation phase is whatever time training happens to
   spend waiting. In the E3 colo run that was 68% of wall-clock.
2. **Exact-target gating starves the engine.** `sample()` requires all groups to carry
   `target_weight_version == current step`, and the collector only generates for targets
   `[N+1 .. N+4]`. Once the window is spawned it pauses itself ("all target weights already exist…
   waiting for weight update" — 19 times in the log). The current step's target is outside the
   window, so a died straggler leaves it permanently incomplete → training waits forever (the
   deadlock; job 4736532 escaped it only because its stragglers survived).
3. **The in-flight cap equals the window** (`num_prompts × max_age` = 64), so during each stall the
   pool of active rollouts only drains. Concurrency saw-tooths from 64 groups to a 1-2 group
   straggler tail — the idle GPUs that got the job reaped.
4. **Net effect:** one full-rollout-latency stall (~31-37 min) every `max_age` steps, all training
   data at maximum staleness (avg age 4.0), 19 steps vs the ~60 achievable at measured decode parity.

### Implementation deltas

- Move the phase-switch trigger into the generation side: switch when the count of complete groups
  reaches `num_prompts_per_step`, not when training's poll loop succeeds.
- Drop exact-target matching: sample any complete groups within the staleness bound
  (freshest-first). `ReplayBufferNew` in `nemo_rl/algorithms/async_utils/replay_buffer.py` is the
  in-repo WIP for exactly this — its docstring notes that target gating "causes generation pauses".
- Decouple the in-flight cap from the age window so the engine stays saturated for the whole
  generation phase; refill rollout slots as they complete.
- Tag trajectories for staleness accounting in a way that tolerates multi-version rollouts (e.g.,
  version at completion, or conservatively the oldest segment's version).

## E4 comparison result (2026-07-12, continuous scheduler implemented and run)

The ideal execution model above was implemented (opt-in `grpo.async_grpo.rollout_scheduler:
continuous`, commit bffaca4c) and run through the full E3 protocol (job 4881517, 8 nodes, fresh SFT
start, validation offline). Offline val accuracy on the fixed val-split:

| step | non-colo | colo target-window (E3) | colo continuous (E4) |
| :---- | :---- | :---- | :---- |
| 5 | 0.274 | 0.274 | 0.272 |
| 10 | 0.253 | 0.259 | 0.275 |
| 15 | 0.257 | (offval job failed) | 0.252 |
| 17 (E4 final) | — | — | **0.281** |
| 19/20 | 0.230 (s20) | 0.252 (s19) | — |
| 25 / 30 / 35 | 0.229 / 0.194 / 0.185 | — | — |

Findings:

1. **The E3 failure modes are gone.** E4: zero buffer-starvation events, zero collector
   self-pauses, mean trajectory age 2.85 (vs pinned 4.0), no reaper kill, clean designed exit at
   the 3h40m checkpoint deadline with 17 optimizer steps (E3 colo: 19 steps, reaped mid-stall).
2. **Per-step reward quality is equivalent across all three arms** (~0.25–0.28 at matched
   steps). Colocated training does not degrade the model relative to non-colocated.
3. **Duration-matched (this doc's stated metric), colo-continuous ends higher: 0.281 vs
   non-colo's 0.185** — but read with care: val accuracy *declines* with optimizer steps in this
   recipe for every arm (non-colo 0.274 → 0.185 over steps 5→35), so at equal wall-clock the arm
   that takes fewer steps ends higher. That is a recipe-level pathology (training is
   anti-correlated with the fixed val-split metric), not evidence of more learning per GPU-hour.
   Fixing/understanding the decline is a prerequisite for a meaningful efficiency verdict.
4. **Throughput is still capped for colo** (~4.6–5.0 steps/h vs non-colo 9.75): the residual
   ceiling is the in-flight cap (`num_prompts × max_age` = 64 rollout groups) × ~30 min rollout
   latency, which bounds completions per engine-hour identically in both colo schedulers; 34
   groups were also lost to staleness eviction. Next lever (E5): decouple and raise the in-flight
   cap (and possibly `max_trajectory_age_steps`), watching KV headroom and eviction rate.

## References:

- [Raising GPU utilization through GPU lag-shaping](https://docs.google.com/presentation/d/14pH7CH1Ruwbh2CKdtlFhwOhTdRo-sCu1cAN-S77NK40/edit?slide=id.p#slide=id.p)  
- [Nemo-RL Async GRPO](https://docs.nvidia.com/nemo/rl/latest/guides/async-grpo.html)  
- [Nemo-RL PR \#2884](https://github.com/NVIDIA-NeMo/RL/pull/2884/changes)
